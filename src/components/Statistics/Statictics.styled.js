import styled from 'styled-components';

const StatisticsWrapper = styled.div`
  display: flex;
  gap: 15px;
  justify-content: center;
`;

const Text = styled.p`
  font-size: 18px;
  font-weight: 500;
  border: 1px solid grey;
  padding: 5px 10px;
`;

export { Text, StatisticsWrapper };
